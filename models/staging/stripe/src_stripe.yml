version: 2

sources:
  - name: stripe
    database: pelagic-force-339905

    schema: jaffle_shop
    tables:
      - name: payments
        discription: Raw Paymnet
        loaded_at_field: '_batched_at'
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}
